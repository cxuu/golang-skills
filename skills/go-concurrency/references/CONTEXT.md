# Go Context Usage

For complete context guidance — parameter placement, struct storage rules,
custom types, context values, immutability, `context.Background()` usage,
and common derivation patterns — see the dedicated
[go-context](../../go-context/SKILL.md) skill.
